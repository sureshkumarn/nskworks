Module :
Module will output the required data to generate the module
Adding HTML, Themeing should be taken care of the layer module


-------------------------
Page :



-------------------------

# to increase number of watch files in node
echo fs.inotify.max_user_watches=582222 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p
